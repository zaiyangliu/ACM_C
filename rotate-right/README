讨论1.2 循环右移k 位问题
老师参与
下面的算法（文字叙述形式），用来将数组a[n]的每个元素都循环地右移k 位，这里1≤k≤n。
算法描述如下：
步骤1） 将数组分成两段，左段长为n-k，右段长为k。
步骤2） 分别将两段逆转（即元素排列次序与原次序相反）。
步骤3） 再数组元素整体逆转。
（1）用C 语言编程实现（不能使用辅助数组）。
（2）求元素移动次数T(n)。要求出准确的解析式（不是大O 记号）。
（3）试证明算法是正确的。
提示，任一数组元素a[i]经算法处理后，其下标变成：(i+k)% n。
