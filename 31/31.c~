#include<stdio.h>
int main(){
  int s[5] = {0};
  int max = 0;
  int min = 0;
  for(int i = 0; i < 5; i++){
    scanf("%d", &s[i]);
  }
  for(int j = 0; j < 5; j++){
    if(s[j] > max)
      max = s[j];
  }
  for(int k = 0; k < 5; k++){
    if(s[k] < min)
      min = s[k];
  }
  printf("%d", min);
  printf("%d", max);
  return 0;
}
